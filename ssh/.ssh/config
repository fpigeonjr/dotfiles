# Machine-local hosts (personal LAN nodes, client infra, etc.)
# Create ~/.ssh/config.local — it is never stowed or committed.
Include ~/.ssh/config.local

Host * !127.0.0.1 !localhost
  UseKeychain yes
  AddKeysToAgent yes
  IdentityFile ~/.ssh/id_ed25519
